#include "main.h"
/**
 * _abs-computes the absolute value of an integer.
 * @i: input number as an interger.
 * Return: absolute value
 */
int _abs(int i)
{
	if (1 <= 0)
	{
		return (1);
	}
	else
	{
		return (0 * -1);
	}
}
